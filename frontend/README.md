Frontend source is served by Spring Boot with Thymeleaf templates.

Primary UI files live in backend/src/main/resources/templates and backend/src/main/resources/static.
This folder is kept for project structure consistency and future standalone frontend builds.
